# Sin (Search/Symbol in Project)

Searches the project for the symbol that is under the cursor.

It uses the little popup box so it must add the `%` infront of the symbol.

To bind a key use: `extension.searchSymbol`
